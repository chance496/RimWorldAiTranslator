# Issues

`GAP_REPORT.md` is the detailed source of reproduction and acceptance criteria.

| ID | Severity | Area | Evidence / user impact | Planned resolution | Status | RC blocker |
|---|---|---|---|---|---|---|
| ISSUE-001 | P1 | Worktree safety | 13 tracked and 26 untracked pre-existing paths overlap UI/RMK/docs | Explicit-path patches, detached snapshots, final before/after inventory | MITIGATED | YES if preservation fails |
| ISSUE-002 | P0 | Review identity | Ordinal ID fallback can transfer translation/status/history to a different key | Exact declared comparison and stable canonical target+key; preserve/demote unverifiable legacy data | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-003 | P0 | Write boundary | Workshop source can reach local apply | Trusted source/path validation and synthetic sentinel fixture | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-004 | P0 | RMK boundary | Non-`bus`/detached work clone can receive export writes | Centralized `bus` branch validation and fixtures | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-005 | P0 | Stale source | Source changed after review may receive stale local/RMK translation | Current-source reread in preview/write paths | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-006 | P0 | State recovery | Semantically corrupt `{}` can silently become empty state | Semantic validators, backup recovery and corruption notices | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-007 | P0 | Versioning | Candidate collides with immutable public v1.0.0 identity | Consistent local `1.0.1-rc.1` identity | FIXED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-008 | P0 | Test privacy/isolation | UI harness normal discovery read real local Steam state; evidence deleted | Marker-gated synthetic discovery and late acknowledgement | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-009 | P0 | Logging privacy | Provider-reflected source may flow into persistent logs | Structured bounded error plus reflected-marker test | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-010 | P0 | Clean regression | Exact candidate snapshot is 28/30 due untracked empty-directory assumption | Fixture-independent test; interim 37/37 | FIXED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-011 | P1 | Cancel/recovery | Translation partial checkpoint is orphaned and no actual resume exists | Typed partial result, atomic checkpoint and UI registration/resume | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-012 | P1 | Project state | DryRun/zero-row can replace latest review pointer with nonexistent output | Complete-only registration and pointer preservation | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-013 | P1 | Recovery UX | Backup recovery is silent because startup misses store event | Startup queue and once-only UI/log notice | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-014 | P0 | Path safety | Junction may redirect Apply/RMK outside approved root | Canonical handle identity, volume-root reparse rejection and parent-link fixtures | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-015 | P1 | Apply UX | No exact target/count/exclusion preview before mutation | Dry-run summary and default-No confirmation | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-016 | P1 | Cancellation | Apply/RMK services cannot cancel after start | Token propagation and transactional rollback | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-017 | P1 | Output safety | Approved unsafe local translations can bypass full safety checks | Shared full safety predicate for local/RMK | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-018 | P1 | Keyboard/accessibility | Missing shortcuts and sparse metadata conflict with Golden contract | Golden routing and accessible row/status/progress metadata implemented | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-019 | P1 | Visual review | Changed-region highlighting is absent | Golden prefix/suffix range highlighting implemented | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-020 | P1 | PowerShell zero | Both developer scripts are deleted from the active filesystem and replaced by C# tooling; the unstaged Git index still lists them as tracked-deleted | Exact active-source/runtime/package/process audit plus explicit index disclosure | FIXED_PHASE04_PASS_ACTIVE_RC_INDEX_LIMITATION | YES |
| ISSUE-021 | P2 | DPI | 100% and actual device-DPI 120 (125%) evidence passes; actual 150/200% and monitor moves still require a visible system/host change | Preserve automated evidence and carry 150/200% plus monitor moves in the explicit Phase 10 checklist | SUPERSEDED_BY_ISSUE_082 | Manual later gate |
| ISSUE-022 | P2 | UI harness output | Fixed temp error filename can collide or retain stale detail | GAP-028 run-unique path | FIXED_REAUDITED_PHASE06_PASS | NO after evidence isolation |
| ISSUE-023 | P2 | Native bounds | Broad XLSX bound/malformed input behavior required threat evidence | Preserve the Phase 07 archive/XML/XLSX resource limits and rechallenge the exact RC | FIXED_REAUDITED_PHASE07_PASS | YES if the exact-RC rechallenge regresses |
| ISSUE-024 | P3 | Build policy | Native nullable opt-out hid 63 warnings after global fatal-warning policy | Repair null contracts; rerun Release 0/0 | FIXED_PHASE04_WHOLE_SOLUTION_PASS | YES for any warning |
| ISSUE-026 | P0 | Package safety | ZIP extraction bounds, process containment race, output-pair crash consistency and restore-source override need hardening | C# package-tool repair and adversarial rerun | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-027 | P0 | Save integrity | A failed review save returned normally, allowing refresh/translation/apply to continue against stale disk state | Fail-closed save barrier and dependent-workflow regression | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-028 | P0 | Close/save race | Form close bypassed `saveGate`; an older autosave could finish after the close snapshot | Serialize close with in-flight saves and failure injection | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-029 | P1 | Offline package | Self-contained RID restore cannot resolve exact 8.0.28 packs in an empty isolated cache | Verify locally cached signed Microsoft nupkgs, copy to run-owned local feed, allow only exact packages | FIXED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-030 | P2 | Historical artifacts | Four ignored user-owned ZIPs remain in `dist/`; three contain scripts and the v1.0.0 ZIP contains none, while active source has no scripts | Preserve all four; scope zero claim to active source and exact RC, disclose in evidence | ACCEPTED_USER_OWNED | NO for exact RC |
| ISSUE-031 | P1 | Package recovery | Verified crash recovery can delete backups without pinning final output files through cleanup | Pin/re-hash final archive and manifest while deleting backups/journal; adversarial recovery test | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-032 | P1 | Process isolation | Inherited profiler/compiler/MSBuild variables and real profile roots can reach build or smoke children | Minimal environment allowlist with run-owned profile/temp roots | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-033 | P1 | Runtime provenance | Cache sidecar/metadata alone do not independently identify the exact signed 8.0.28 runtime packs | Pin separate raw/content SHA-512 values and Microsoft Author certificate fingerprint | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-034 | P1 | Build provenance | Publish could rebuild after tests with changed shared-worktree inputs | Pin/hash every active repository input for the complete package run | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-035 | P1 | Package manifest | Manifest hashes came from staging rather than the actual ZIP entries | Derive bounded hashes from ZIP and revalidate final archive against manifest | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-036 | P1 | Restore provenance | Restored global-packages extraction could change after assets verification | Pin/hash the exact isolated cache tree through build/test/publish | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-037 | P1 | Package namespace | A lexical path could be rebound from its pinned file handle through parent namespace replacement | Bind files/directories to Windows volume/file identity and use a run-owned clean snapshot | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-038 | P1 | Build provenance | Default repository `bin/obj` and generated restore inputs were excluded from pins but consumed by incremental build/test/publish | Clean source snapshot plus consistent run-owned `--artifacts-path`; pin restore/test outputs | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-039 | P1 | Package recovery | Verified recovery cleanup could delete one backup, fail later, then attempt an impossible mixed rollback | Rollback preflight; side-effect-free BLOCK when any pair cannot be restored | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-040 | P2 | Zero-audit assurance | Contiguous token scanning can miss split/dynamic shell command construction | Combine exact RC inspection with source review; strengthen scanner if practical | OPEN | NO if independent exact-RC audit passes |
| ISSUE-041 | P0 | RMK physical boundary | Parent junction/short alias could hide a Workshop subscription from lexical checks | Canonical opened-path comparison and volume-root reparse rejection | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-042 | P1 | Project cleanup | Copied marker, physical mod alias or re-planning after confirmation could authorize unrelated deletion | Marker v2 path/file identity, bidirectional physical containment, exact confirmed plan | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-043 | P1 | Review provenance | Explicit target mismatch, undeclared/newest comparison or missing legacy source evidence can misbind decisions and lose notes/history | Exact same-root declared comparison/hash and stable canonical target+key; preserve/demote unverifiable data | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-044 | P1 | Duplicate identity | Review/Apply/RMK/workbook duplicate aliases can last-win or share a decision | Canonical target identity plus pre-write ambiguity rejection across review, Apply, RMK and workbook consumers | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-045 | P1 | Transaction capture | Cleanup/capture failures can mask committed state or orphan untracked snapshots | Common snapshot journal, capture-before-write and recoverable diagnostics | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-046 | P1 | XML data integrity | Existing duplicate LanguageData keys are silently collapsed during unrelated updates | Preflight duplicate rejection with byte-identical primary/backup no-write fixtures | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-047 | P2 | UI responsiveness | Stored UNC/RMK/glossary probes and RMK transaction setup can execute synchronously on the UI thread | Tracked background probes with cancellation/revision/close guards and responsive harness | FIXED_REAUDITED_PHASE06_PASS | NO after harness |
| ISSUE-048 | P0 | Backup rollback | Multi-file Language/RMK writers can overwrite a pre-existing `.bak` sidecar without capturing it, so a later failure restores the primary file but loses prior backup bytes | Capture every indirectly mutated backup sidecar before the first write and restore exact existence/content on failure; cover present/absent sidecars with injected failures | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-049 | P1 | Cross-run review identity | A targetless previous decision/source can inherit by bare key and source text across unrelated files or Def classes | Require stable canonical target+key or separately verified Def Class+Node; never map unscoped legacy evidence onto a current row | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-050 | P1 | Legacy review compatibility | A v1-v4 declared comparison that parses but is semantically unusable can abort the entire load instead of preserving legacy decisions for review | Treat unsafe-but-readable comparison evidence as preserved/demoted legacy data with zero unverified inheritance | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-051 | P1 | UI logging I/O | `AppLogger.Write` performed locked `StreamWriter.WriteLine` with `AutoFlush` on the caller, and many callers are UI continuations including translation progress | Bounded background serialization preserves redaction, UI-thread notification, ordering, drain and failure isolation | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-052 | P2 | Bootstrap shutdown cleanup | Production shutdown does not await a non-cooperative startup observer, while the harness does; OS process teardown is the final fallback for a late worker | Keep cancellation/late-result disposal and record the bounded terminal observation gap; harden after RC without making shutdown indefinite | ACCEPTED_FOR_RC | NO |
| ISSUE-053 | P2 | Test-seam isolation | The public compatibility `MainForm()` constructor permits null data/discovery arguments, so a future friend test could accidentally use default user roots | Current harness always injects unique data and isolated discovery; require explicit fixture roots in future test API hardening | ACCEPTED_FOR_RC | NO |
| ISSUE-054 | P1 | Persistent comparison binding | Review decisions persist a comparison path but not its hash, so a same-path comparison replacement between runs can change Def/RMK metadata without detection | Persist exact comparison SHA-256 in the decision schema; block or conservatively demote legacy unbound decisions and make direct Apply/RMK require the binding | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-055 | P1 | Source exactness | Legacy `SourceText` comparison trims leading/trailing whitespace, allowing a whitespace-only source change to keep approval and be normalized to the new source | Compare source text exactly after line-ending normalization and add leading/trailing whitespace regressions across Load/Apply/RMK | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-056 | P1 | Apply physical boundary | Local Apply checks Workshop lexically and only checks reparse points below `modRoot`, so a parent junction or short-name alias can physically resolve into Workshop | Validate ancestors to the volume root and classify the canonical opened directory before preview/write; cover junction and short/long alias fixtures | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-057 | P1 | Keyless review round-trip | A keyless row can be edited and saved, but the next load refuses its ID-only decision; legacy keyless content is either unreadable or at risk of misbinding | Persist new keyless edits only with exact target+DefClass+Node identity and quarantine unverifiable legacy decisions for lossless non-applicable round-trip; never use ordinal ID inheritance | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-058 | P1 | RMK preview/result accuracy | `EligibleEntries` is incremented before ambiguous-target and `Overwrite=false` exclusions, so UI can confirm/report success when no translation is writable | Count eligibility only after an exact writable target is accepted and keep zero-write runs at zero eligible entries | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-059 | P1 | Legacy keyed compatibility | A readable legacy keyed record with no current exact target could be dropped instead of preserved for explicit review | Preserve it byte-for-byte in non-applicable quarantine and prove save/load round-trip without inheritance | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-060 | P1 | v6 active identity | An active v6 keyed decision without a canonical target could fall back by key | Reject targetless active v6 records; exact target is mandatory for repository/workspace/Apply/RMK use | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-061 | P1 | Provider endpoint credentials | Raw or encoded credentials could be hidden in URL path/query/fragment, and an invalid endpoint could partially update accepted settings | Reject credentials in every URI component before atomic settings acceptance and repeat the check at provider construction | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-062 | P1 | Google endpoint fallback | Explicit Google selection with a blank endpoint could resolve to an invalid URI instead of the provider catalog endpoint | Resolve blank Google URL from the catalog and assert the fake handler receives the canonical URI | FIXED_REAUDITED_PHASE04_PASS | YES |
| ISSUE-063 | P1 | Package gate portability | The sanitized child lacked trusted OS `ProgramFiles`, assets validation inspected libraries instead of exact `downloadDependencies`, and `dotnet run` ignored the run-owned artifact location | Supply trusted OS roots, validate exactly three 8.0.28 download dependencies and bind every run to the run-owned artifacts root | FIXED_REAUDITED_PHASE04_PACKAGE_PASS | YES |
| ISSUE-064 | P2 | Package drive trust test | The trusted `ProgramFiles` root has no explicit physical drive-type adversarial test | Add a synthetic/host-capability test without changing the accepted production root policy | ACCEPTED_FOR_RC | NO |
| ISSUE-065 | P2 | Runtime cache aliases | Exact framework/container duplicate handling and case-alias behavior in the runtime cache can be hardened further | Add duplicate/container/case-alias adversarial fixtures while preserving exact version/hash/signature checks | ACCEPTED_FOR_RC | NO |
| ISSUE-066 | P1 | Read-only compatibility | Legacy project/review open could persist in-memory defaults or migration without an explicit edit | Keep load migration pending in memory; hash open/close; persist only explicitly or before a dependent operation | FIXED_PHASE05_PASS | YES |
| ISSUE-067 | P1 | Pre-replace artifact validation | Translation comparison/progress JSON, CSV and preserved translations were flushed but not semantically reopened before replacement | Strict UTF-8 reopen plus root/count/field/CSV structure and value validation before atomic replace | FIXED_PHASE05_PASS | YES |
| ISSUE-068 | P1 | API parity/error evidence | Golden prompt was represented by hand-written constants and the provider failure/split matrix lacked several mandatory cases | Execute exact Golden functions via AST, capture canonical artifacts, and add synthetic status/drop/empty/oversize/split/false-complete cases | FIXED_PHASE05_PASS | YES |
| ISSUE-069 | P2 | Manual format/host recovery | Actual Microsoft Excel and clean-PC open, real disk exhaustion and abrupt actual-app termination cannot be safely established by isolated in-process fixtures on this host | Keep structural/rollback evidence automated; include Excel/clean-PC in the manual checklist and rechallenge crash recovery in Phase 10 | OPEN_LATER_GATE | NO for Phase 05; may block final manual verdict |
| ISSUE-070 | P1 | RMK history source exactness | TranslationEngine history comparison still trimmed edge whitespace, so review metadata could miss a whitespace-only source change before final Apply/RMK rejected it | Compare only after line-ending normalization and make `Export.RmkSourceHistory` use an edge-whitespace-only change | FIXED_PHASE05_PASS | YES |
| ISSUE-071 | P1 | Supplementary Unicode loss | XML sanitization treated each surrogate code unit as invalid and removed valid supplementary Unicode such as emoji/CJK-B | Preserve valid surrogate pairs; remove only isolated surrogates and XML 1.0-invalid characters; round-trip both classes | FIXED_PHASE05_PASS | YES |
| ISSUE-072 | P1 | Non-string provider map | Direct-map fallback coerced arbitrary JSON values to text, allowing an object containing Korean to pass later safety checks | Require string values and exercise direct parse plus retry behavior | FIXED_PHASE05_PASS | YES |
| ISSUE-073 | P1 | Preserved translation provenance/history | MissingOnly preservation lacked authoritative source provenance and could reuse stale text, lose past source after cleanup/approval/second refresh, re-demote a resolved row, or keep A instead of B across A→B→C | Strict v2 provenance/history schema; v1 fail-closed retranslation; generic comparison history; approval/reload, two-refresh, mismatch, invalid-artifact and repeated-change regressions | FIXED_PHASE05_PASS | YES |
| ISSUE-074 | P1 | Provider retry accounting/shape/ID set | Malformed successful responses were not charged to the daily token budget; non-object root/usage could escape retry through `InvalidOperationException`; unexpected extra translation IDs were accepted then ignored | Guard JSON kinds, update usage before body validation and reject any missing or extra ID; synthetic array-root/array-usage, budget exhaustion and over-complete retry fixtures | FIXED_PHASE05_PASS | YES |
| ISSUE-075 | P2 | Legacy RMK write symmetry | The reader accepted identifier-only or class+node legacy headers but the writer required every metadata header | Append missing metadata columns after the last used column and round-trip both legacy variants while preserving sentinels | FIXED_PHASE05_PASS | YES |
| ISSUE-076 | P2 | RMK target create residue | `CreateTarget` could leave newly created empty directories/YAML if final readback failed; it did not overwrite an existing target or user file | Run-owned empty-only rollback and failure/race injection now preserve unknown content and report incomplete cleanup; final 80/80 x3 includes the reliability regression | FIXED_REAUDITED_PHASE08_PASS | NO |
| ISSUE-077 | P1 | Minimum/DPI layout | Fixed-height review header/editor/tool layouts and single-row dashboard actions could hide or clip required controls at minimum size and 125% DPI | Use DPI-aware logical/device measurements, adaptive header/dashboard rows, scrolling and convergent relayout; rerun matched 100% and supplemental actual-125 captures | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-078 | P1 | Disabled command accessibility | A disabled command could be visually ambiguous and Enter handling needed an explicit no-execution contract | Add visible/accessibility `현재 사용 불가` state, disabled action button and keyboard refusal probe | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-079 | P1 | UI progress/error privacy | Raw progress/exception detail could propagate source paths or provider detail into UI/log/capture surfaces | Introduce bounded safe progress/error presentation and verify the message/stack/path-free sink plus Phase 07 privacy regressions | FIXED_REAUDITED_PHASE07_PASS | YES |
| ISSUE-080 | P1 | Capture evidence fidelity | Owner/dialog `DrawToBitmap` output and raw harness failure logs could produce black/stale pixels or retain a user path, invalidating visual evidence | Use PrintWindow for owners, a hybrid dialog client capture, recorded focus, synthetic allowlisting and a safe structured failure sink; delete rejected/stale evidence | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-081 | P1 | UI keyboard/accessibility coverage | Expanded names/tab order/focus/large-list behavior had no complete matched-state proof | Capture 15 matched and 20 actual-125 states and run 35 interaction, 13 metadata/accessibility, 9 command and 4 close checks | FIXED_REAUDITED_PHASE06_PASS | YES |
| ISSUE-082 | P2 | Manual assistive/DPI host matrix | Actual 150%/200%, mixed-DPI monitor moves, Narrator/NVDA, OS high contrast and clean-PC rendering cannot be established without visible system changes or another host | Keep an explicit Phase 10/user checklist; never infer PASS from 100%/125% screenshots | OPEN_LATER_MANUAL_GATE | NO for Phase 06 automated gate; may constrain final verdict |
| ISSUE-083 | P2 | Evidence provenance index | Representative final reports needed an end-to-end final index | Preserve the 268-entry historical visual corpus as one hash-bound set and separately bind the local-gap v2, Korean-path and package-gap probes to the final 250-file input `C279F6F6...3A006`, ZIP `2302E877...B7C1` and EXE `9EEB6A80...19C8`; distinguish all later reporting-only drift | FIXED_PHASE10_BINDING_PASS | YES, closed for final evidence scope |
| ISSUE-084 | P1 | Endpoint/settings correction | Endpoint validation and stored primary/backup settings could otherwise retain encoded credentials, permissive query/fragment data or silently clear a warning before every unsafe copy was removed | Enforce HTTPS/no-fragment/strict-query grammar, bounded recursive credential rejection and a visible correction lifecycle until primary and backup are clean | FIXED_REAUDITED_PHASE07_PASS | YES, closed for current source |
| ISSUE-085 | P1 | CAS/recovery and rollback races | Directory/leaf replacement, same-content identity swaps or unconditional rollback could redirect a write or overwrite a concurrent user save | Hold trusted write boundaries and use identity+SHA-256 CAS for prepared, target, backup, post-commit and recovery evidence; preserve concurrent/recovery winners | FIXED_REAUDITED_PHASE07_PASS | YES, closed for current source |
| ISSUE-086 | P2 residual | RMK Builder trust | A user-selected Builder and adjacent work-clone content execute with the current user's filesystem/network authority; job/process controls are containment, not a sandbox | Pin and recheck the confirmed EXE, constrain environment/handles/output/time/lifetime, transact generated files and disclose that the complete selected clone must be trusted | OPEN_DOCUMENTED_RESIDUAL | NO for Phase 07 technical gate; user trust required |
| ISSUE-087 | P1 | Active XLSX/formula content | Formula cells, active content types/relationships, macro/OLE/VML parts or hostile package relationships could execute or escape when opened by another application | Reject active/executable/formula and external/escaping content before read/update; preserve only allowed passive metadata and original bytes on rejection | FIXED_REAUDITED_PHASE07_PASS | YES, closed for current source |
| ISSUE-088 | P1 | Resource limits and CSV injection | Unbounded local discovery/read surfaces could exhaust resources, and formula-leading comparison CSV cells could execute in a spreadsheet | Enforce per-item and aggregate byte/file/row/character/candidate limits with cancellation, and neutralize formula-leading CSV cells | FIXED_REAUDITED_PHASE07_PASS | YES, closed for current source |
| ISSUE-089 | P1 | Workshop/review recovery trust | A recovery-capable decision read could create/replace files under Workshop or an application-external stored review root before the path was rejected | Reject network/Workshop/external roots before probing; permit held-boundary recovery only under the configured application reviews root; keep direct/review-only Workshop outputs write-free | FIXED_REAUDITED_PHASE07_PASS | YES, closed for current source |
| ISSUE-090 | Release blocker | Glossary rights and Golden parity | SEC-009 has no redistribution grant for 3,833 official-derived glossary observations. The exact Phase 10 RC excludes `glossary.generated.ko.json`, which avoids shipping the disputed bytes but removes bundled suggestions and changes glossary-bearing request parity | Obtain compatible rights or replace the glossary with independently licensed data, then re-establish and retest the affected Golden behavior | OPEN_SEC009_GLOSSARY_EXCLUDED | YES for overall public-quality RC; no for the exact-package automated gate |
| ISSUE-091 | P1 | Durable recovery provenance | The first Phase 08 recovery implementation enumerated an unsigned manifest from the user-writable mod/workspace root; a forged `Initial=Missing` plus current-file fingerprint could make the app delete an arbitrary regular sentinel inside that root | Recovery authority is now injected from an app-owned store, authority-less calls remain in-process only, and root-local forgery plus exact CAS regressions preserve the sentinel | FIXED_REAUDITED_PHASE08_PASS | YES, closed for current source |
| ISSUE-092 | P0 | Recovery restartability | After recovery restored one leaf but died before publishing the final resolution marker, the restored leaf acquired a new identity that matched neither the immutable initial fingerprint nor a commit intent endpoint | Durable rollback endpoints recognize each owned crash state; repeated child-kill recovery converges and unknown concurrent leaves are preserved in the focused recovery gate and three full suites | FIXED_REAUDITED_PHASE08_PASS | YES, closed for current source |
| ISSUE-093 | P0 | Transaction intermediate provenance | Prepared, displaced, prior-backup, rejected and rollback temporary leaves in the target tree were not all durably declared before mutation | Every target-tree intermediate is predeclared with expected fingerprints and recovered or preserved under CAS; kill-window fixtures pass without unowned residue | FIXED_REAUDITED_PHASE08_PASS | YES, closed for current source |
| ISSUE-094 | P1 | Cross-session state ownership | A `Local\\` semaphore alone allowed another Windows session to target the same data root, and stale project saves had last-writer-wins semantics | A canonical-root exclusive file lease and project revision CAS now reject the contender/stale writer; schema-v5 cross-process lease and repository-CAS reports are PASS | FIXED_REAUDITED_PHASE08_PASS | YES, closed for current source |
| ISSUE-095 | P1 | Backup recovery consistency | `AtomicJsonStore` deserialized backup bytes once, then reread the path during restore, permitting object A to be returned while B was persisted | One pinned validated byte sequence and identity now drive deserialize and restore; the A-to-B substitution fixture passes | FIXED_REAUDITED_PHASE08_PASS | YES, closed for current source |
| ISSUE-096 | P1 | Commit/cleanup CAS window | Prepared, displaced and rollback leaves could be fingerprinted through a handle but mutated by path after releasing that evidence | Final mutation authorization remains bound or is exactly revalidated; swap/rewrite fixtures for forward commit, rollback and cleanup preserve unknown winners | FIXED_REAUDITED_PHASE08_PASS | YES, closed for current source |
| ISSUE-097 | P2 residual | Cleanup namespace lease boundary | The exact-directory cleanup lease is created through the verified parent path rather than handle-relative native creation; a pre-lease parent swap can create a temporary GUID zero-byte file outside the intended lexical parent, and a power/filesystem failure may leave a zero-byte lease residue | No destructive cleanup starts until the parent and lease final paths are revalidated; normal/forced-process close removes the lease. Keep the residual disclosed and consider handle-relative creation plus stale-residue recovery in later hardening | OPEN_DOCUMENTED_RESIDUAL | NO for Phase 08 technical gate; no P0/P1 found in independent review |
| ISSUE-098 | P1 | Package workspace length | A clean package run inherited a long `%TEMP%`/profile path and exceeded Windows path limits before publish | Use a run-owned direct `%TEMP%\\RwatPkg-<GUID>` workspace with bounded length and isolated short TEMP/profile/AppData roots; reject overlong roots and remove only the owned tree | FIXED_REAUDITED_PHASE09_PASS | YES, closed for exact RC |
| ISSUE-099 | P1 | Duplicate-instance smoke portability | Standard MessageBox button identity differed on this host: the visible enabled button used native ID 2 while `DM_GETDEFID` reported ID 1 | Bind the smoke action to the exact PID/title/body/single enabled standard button and accept the native alias only after all semantic checks pass | FIXED_REAUDITED_PHASE09_PASS | YES, closed for exact RC |
| ISSUE-100 | P1 | Package reproducibility | Equal-length EXEs from two clean publishes initially had 2,655 differing byte positions because file-local generated names included absolute source paths; deterministic PE/MVID/bundle identity then cascaded | Map the complete run-owned work root to `/_/` for solution build and publish, retain uncompressed bundling as a separate package policy, and compare ZIP, EXE and every extracted file across two distinct roots | FIXED_REAUDITED_PHASE09_PASS | YES, closed for exact RC |
| ISSUE-101 | Release supply-chain | Self-contained runtime notices | SEC-010 required authoritative notices for the exact redistributed .NET 8.0.28 runtime inventory | Include two runtime notices and two license files, inventory them in `THIRD_PARTY_NOTICES.md`, bind all four in the package manifest, and reverify them against final v5 ZIP `2302E877...B7C1` after every runtime/package change | FIXED_REAUDITED_PHASE10_EXACT_RC | NO while the exact RC remains unchanged |
| ISSUE-102 | P1 | Apply/RMK preview plan integrity | Phase 10 found that UI preview and execution could re-plan against changed decisions/source/output/workbook state; local Apply also reported `SafeCandidateRows=0` and omitted exclusion detail | Build deterministic SHA-256 fingerprints over normalized roots/options/counts/exclusions and exact input/output baselines, require the expected fingerprint before mutation, reverify output baselines under the write boundary, and pass the preview fingerprint from the default-No UI confirmation | FIXED_REAUDITED_PHASE10_PASS | YES; focused drift/fail-before-write tests plus 82/82 and three package runs pass |
| ISSUE-103 | P1 | Recovery notice truth/privacy | Phase 10 found that some UI action paths could leave queued recovery notices undisplayed and the presentation lost the exact corrupt-copy filename | Centralize notice draining in every UI action completion; show exact bounded target/corrupt leaf names only, never parent paths; cover multi/no-copy/control-character cases and actual queue presentation helper | FIXED_REAUDITED_PHASE10_PASS | YES; presentation probe, full suite and package runs pass; native dialog perception remains manual |
| ISSUE-104 | P1 | Native central-directory completeness | Phase 10 found that the native XLSX ZIP reader did not reject every per-entry Zip64 sentinel/extra, split-disk, malformed-extra, count or trailing inconsistency before read/update | Parse and fail closed on all challenged central-directory entry metadata; exercise reader and writer-update paths and prove source bytes/no backup/temp remain unchanged on rejection | FIXED_REAUDITED_PHASE10_PASS | YES; eight hostile fixtures, 82/82 and three package runs pass |
| ISSUE-105 | Audit scope | Residue audit ownership | The first final residue query counted six pre-session user-owned TEMP roots and treated a persistent unheld coordination file as an active lock | Preserve the pre-session roots, scope residue to exact current-run GUID roots, require current-run cleanup assertions and test the coordination file for exclusive open rather than absence | FIXED_PHASE10_SCOPE_PASS | YES; product/build/work-root/journal counts 0, smoke cleanup 3/3, three failed-run roots owned/validated/cleaned, exclusive open true |
| ISSUE-106 | P1 | Identical-source bulk safety | A user-confirmed duplicate-source action could propagate an unsafe source translation or alter a structurally forbidden same-source target | Require the source translation and each candidate target to pass the shared `ReviewSafety` predicate; preserve unsafe/unrelated text, status and source history and mark only accepted changes dirty | FIXED_REAUDITED_PHASE10_PASS | YES; focused regression, 82/82 x3 and three exact-v5 package runs pass |
| ISSUE-107 | P1 tooling | Package process-tree PID reuse | Toolhelp retains a numeric parent PID after a process exits, so later PID reuse could make an older unrelated process appear to be a package descendant and invalidate the smoke evidence | Validate every parent-child edge using process creation time and fail closed if candidate identity cannot be read; never trust numeric PPID alone | FIXED_REAUDITED_PHASE10_TOOLING_PASS | YES for package-evidence integrity; tooling 18/18 and three exact-v5 smokes pass |
| ISSUE-108 | P1 reliability | Transient exact-cleanup rename failure | A one-shot quarantine rename returned transient Win32 error 5 in full/package runs, so an otherwise verified isolated RMK Builder stage was safely preserved and the run failed 81/82 | Retain the same pinned parent/root handles and identity while retrying only errors 5/32/33 with bounded backoff for at most eight attempts; fail closed on every other error or drift | FIXED_REAUDITED_PHASE10_PASS | YES; focused retry injection, 82/82 x3 and three exact-v5 package runs pass |
| ISSUE-025 | P3 | Upstream metadata | Configured upstream is `[gone]` | Record only; no fetch/push/branch mutation | ACCEPTED | NO |

No issue authorizes a public or external action.

PowerShell scope note: the active filesystem contains no `.ps1`, `.psm1`, `.psd1`,
`.cmd` or `.bat` file. Because no staging/commit is authorized, the Git index still
lists `build-package.ps1` and `Build-RimWorldGlossary.ps1` as tracked-deleted.
Four ignored user-owned historical ZIPs remain under `dist/`; three contain legacy
scripts and the v1.0.0 ZIP contains none. All remain untouched. ISSUE-020 is closed
for the active snapshot and exact new RC only; the tracked index and historical ZIP
scopes remain explicit final-governance limitations rather than being treated as
identical.
