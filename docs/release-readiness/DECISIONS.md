# Decisions

| ID | Date | Decision | Evidence/context | Alternatives considered | Compatibility/security impact | Reversible? |
|---|---|---|---|---|---|---|
| DEC-001 | 2026-07-13 | Treat all initial tracked/untracked changes as user-owned and patch only explicit in-scope paths. | Initial status has 13 tracked and 26 untracked paths overlapping UI/RMK/docs. | Reset/restore/stash/commit user work were rejected. | Prevents loss and preserves provenance. | Yes |
| DEC-002 | 2026-07-13 | Use detached Golden and candidate worktrees under a unique `%TEMP%` root; reproduce the current candidate with an explicit allowlist and hash checks. | Main worktree is dirty; protocol requires separate safe workspaces. | Building directly in the dirty tree only; cloning/fetching from network. | Avoids external access and isolates generated output. | Yes |
| DEC-003 | 2026-07-13 | Historical docs, reports and `dist` are context only, never current PASS evidence. | Governance requires evidence from this execution. | Reusing old 30/30 and package claims was rejected. | Prevents false confidence and stale-artifact release. | Yes |
| DEC-004 | 2026-07-13 | Run heavy build/UI/benchmark operations sequentially and stop on freeze/WHEA/storage symptoms. | Prior worklog records host instability; current host has 13.7 GiB RAM. | Parallel heavy gates would be faster but risk recurrence. | Slower execution, safer host and evidence integrity. | Yes |
| DEC-005 | 2026-07-13 | Keep actual display scaling unchanged and record unavailable DPI combinations as manual checks. | System changes are explicitly prohibited. The final current-source capture later reported device DPI 120 without Codex changing the host setting. | Changing Windows scaling automatically was rejected. | No product compatibility change; honest evidence boundary. | Yes |
| DEC-006 | 2026-07-13 | Reject and delete any UI evidence that observes non-fixture local data, even if the access was read-only. | Candidate UI harness performed normal Steam discovery despite isolated app-data. | Redacting names while retaining the screenshots was rejected because execution provenance was invalid. | Protects user data and prevents contaminated PASS evidence. | Yes |
| DEC-007 | 2026-07-13 | Capture Golden UI with a separate exact-commit worktree containing only `DrawToBitmap` capture instrumentation. | The original `CopyFromScreen` run captured an unrelated foreground window. The replacement ran the same synthetic scenarios with background discovery disabled. | Keeping the invalid images or modifying Golden functional logic was rejected. | Produces privacy-safe state evidence without changing baseline behavior. | Yes |
| DEC-008 | 2026-07-13 | Treat Phase 03 as an inventory/planning PASS while retaining every candidate FAIL as an RC blocker. | Golden 20/20 and 15 UI states establish the contract; candidate clean regression/UI isolation exposed actionable gaps. | Requiring candidate parity before allowing implementation would make the phase circular. | Allows evidence-backed implementation without overstating product readiness. | Yes |
| DEC-009 | 2026-07-13 | Keep WinForms for this RC and improve it in place. | Golden is WinForms hosted from PowerShell and the C# candidate already has a native WinForms App/UI harness; no reusable XAML exists. | WPF/Avalonia rewrite would add framework and UI regressions unrelated to parity. | Lowest-risk path for behavior, accessibility and package compatibility. | Yes |
| DEC-010 | 2026-07-13 | Use `1.0.1-rc.1` as the local successor RC identity, with Windows file/manifest version `1.0.1.0` and compatibility assembly version `1.0.0.0`. | Public v1.0.0 is immutable; this task cannot replace it or approve a final public version. | Reusing 1.0.0 was prohibited; 1.1.0-rc.1 implied a larger product-version decision than needed. | Unambiguously separates local artifacts while preserving assembly binding compatibility. | Yes |
| DEC-011 | 2026-07-13 | Register only a non-empty, complete review artifact under the application review root; preserve the previous latest pointer for DryRun, zero-row and incomplete/cancelled runs. | A returned path alone did not prove that review files existed. | Registering every engine return was rejected. | Prevents broken resume pointers while retaining validated partial checkpoints. | Yes |
| DEC-012 | 2026-07-13 | Treat Workshop/subscription inputs as permanently read-only and require current-source revalidation plus a validated RMK `bus` work clone immediately before any apply/export. | Golden contract and P0 boundary gaps; synthetic stale/Workshop/branch fixtures. | Trusting stored project paths or frozen comparison rows was rejected. | Prevents stale translation and writes outside explicit local/RMK work targets. | Yes |
| DEC-013 | 2026-07-13 | Replace both developer PowerShell scripts with BCL-only C# tools before deleting them; glossary discovery is explicit opt-in and package creation is fixed to offline Release win-x64. | Runtime was already C#, but source/build/local-package still contained two scripts. | Hiding/renaming scripts or embedding PowerShell was prohibited. | Enables recursive source/package/process zero audit without reducing gates. | Yes |
| DEC-014 | 2026-07-13 | Keep API keys in a hidden in-memory editor, show only a bullet/count view by default, and require a timed explicit reveal. | Golden supports multiple keys; an unmasked multiline field created screenshot/shoulder risk. | Persisted encryption or removing multi-key entry would change the product contract. | No key persistence; lower casual disclosure risk; fully reversible UI choice. | Yes |
| DEC-015 | 2026-07-13 | Match Golden keyboard and diff behavior directly: prefix/suffix changed-range highlighting and the documented F2/F3/F5/F6/F9, Ctrl/Alt/NumPad variants. | Exact Golden handler and `Get-RimWorldSimpleDiff` were inspected in the detached baseline. | Inventing a new shortcut/diff model was rejected. | Restores parity with bounded, deterministic UI work. | Yes |
| DEC-016 | 2026-07-13 | Move isolated-discovery acknowledgement and temporary preserved-translation mutations out of `MainForm` into narrow Core services with owned-root validation. | Phase 04 forbids direct file mutation in UI handlers. | Leaving direct `File`/`AtomicFile` calls in the form was rejected. | Makes write ownership testable and blocks out-of-root deletion/registration. | Yes |
| DEC-017 | 2026-07-13 | The C# glossary tool intentionally has no `IncludePatches` mode. `Patches` content is excluded, not silently treated as a normal Def source. | Root invariants state that the final merged game result cannot be established from `Patches`, so it must not be auto-translated like ordinary Defs. | Porting the legacy optional switch would preserve an unsafe behavior; silently accepting it was rejected. | This is a safety correction governed by the current public-quality contract, not an RC feature reduction. | Yes, only if a future evidence-backed patch evaluator is designed |
| DEC-018 | 2026-07-13 | Enable SDK `AnalysisMode=Recommended` with fatal warnings; retain narrow analyzer exceptions only where changing legacy global Native public types/P/Invoke visibility or service abstraction return shapes would break compatibility. | Initial activation exposed 33 Native and 41 Core findings; correctness/culture/cancellation findings were fixed in code. | Global suppression or API-wide static/concrete rewrites were rejected. | Raises compiler coverage without breaking v1.0.0 consumers or coupling UI composition helpers to concrete controls. | Yes |
| DEC-019 | 2026-07-13 | Treat the external RMK Builder as a cancellable transaction over `LoadFolders.xml` and `ModList.tsv`, and do not report completion unless the process exits, both files change, and XML/TSV validation succeeds. | UI audit found an untracked process and no rollback; synthetic failure/cancel/success child-process regression now passes. | Time/length observation without recovery and an independent UI task were rejected. | Prevents cancellation/failure from leaving a partially updated RMK work clone. | Yes |
| DEC-020 | 2026-07-13 | Package only from a run-owned clean source snapshot and run-owned `--artifacts-path`; seal every consumed restore/test/publish input and derive the manifest from the final ZIP entries. | Shared dirty-tree `bin/obj`, source changes after test and staging-derived hashes could make the tested source differ from the published bytes. Tooling self-test 13/13, the complete offline package E2E and independent re-audit found no remaining P0/P1. | Building incrementally in the shared worktree or trusting staging hashes was rejected. | Gives one auditable source-to-ZIP provenance chain without staging or committing user work. | Yes |
| DEC-021 | 2026-07-13 | Admit local runtime packs only after exact version, raw SHA-512, NuGet content hash and Microsoft Author certificate verification; run child processes with a strict environment and contain them in a Windows kill-on-close job. | Exact cached 8.0.28 packs have distinct raw/content hashes and verified Microsoft/NuGet signatures; inherited profile/profiler/build variables were an identified supply-chain boundary. | Network restore, cache metadata alone and inherited ambient environment were rejected. | Keeps package verification local/offline and prevents an unverified runtime or ambient build hook from entering the RC. | Yes |
| DEC-022 | 2026-07-13 | Use canonical opened paths plus Windows volume/file identities for package namespace pins, RMK writable-workspace classification, local Apply classification and project-cleanup confirmation; marker v2 binds the project, mod and review identities. | Lexical paths, short names, parent junctions, copied markers and confirmation-time replanning could redirect a later mutation. ISSUE-056 extended the policy to every local Apply ancestor/root; physical Workshop and identity-swap fixtures now pass. | Prefix checks and marker text alone were rejected. | Blocks the tested alias/reparse substitution classes; same-user final check-to-use races remain documented P2 rather than hidden. | Yes |
| DEC-023 | 2026-07-14 | Persist the exact SHA-256 of the same-root declared comparison and bind decisions to that byte identity plus stable canonical target+key (or separately verified Def Class+Node); never use ordinal or unscoped bare-key inheritance. Preserve unverifiable legacy text/note/history but demote it to non-apply state. | Adversarial fixtures found unrelated newest comparisons, same-path replacement across restart, cross-file bare keys and targetless legacy evidence could transfer decisions or be lost. The v6 schema/repository/workspace/Apply/RMK now require and verify the exact comparison hash and target identity; settled regression is 54/54. | Best-effort newest-file, path-only, ordinal and global bare-key fallbacks were rejected; aborting every readable legacy review was also rejected. | Fails closed against cross-row/cross-run translation while retaining user-authored legacy data for explicit review. | Yes |
| DEC-024 | 2026-07-14 | Start production with a lightweight message-loop bootstrap, construct services/state on a worker, and transfer explicit ownership to `MainForm` only after success; cancel/dispose failed or late results. | `new AppServices()` and initial state load before `Application.Run` performed filesystem work before the UI could paint. Focused slow-bootstrap/close modes exit 0. | A hidden/opaque MainForm and synchronous pre-loop construction were rejected. | Keeps startup responsive and gives failure/cancellation one ownership boundary without changing WinForms. | Yes |
| DEC-025 | 2026-07-14 | Serialize persistent log writes through a bounded background writer, retain redaction and synchronous in-memory subscriber notification, isolate sink/subscriber failures, and drain accepted writes through the existing close barrier with a bounded terminal fallback. | `StreamWriter.WriteLine` with `AutoFlush` ran on UI continuations; slow/failing storage could freeze progress or close. Secret-marker redaction, close drain, the settled 54/54 suite and all 10 isolated lifecycle modes passed independent re-audit. | Unbounded buffering, caller-thread disk flush and indefinite shutdown waits were rejected. | Removes persistent storage latency from UI callers while bounding memory and shutdown time. | Yes |
| DEC-026 | 2026-07-14 | Treat source text as exact after normalizing line-ending representation only; never trim leading/trailing spaces or tabs when deciding whether approval remains valid. | Independent audit found legacy `SourceText` verification could normalize a whitespace-only source change into an unchanged row (ISSUE-055); leading/trailing whitespace and line-ending-only fixtures now pass across Load/Apply/RMK. | Human-friendly trimming was rejected because whitespace can be user-visible or syntactically meaningful. | Conservatively demotes changed rows and prevents stale approval/apply while retaining CRLF/LF compatibility. | Yes |
| DEC-027 | 2026-07-14 | Give every newly saved keyless decision a v6 stable identity composed of canonical target plus verified Def Class+Node; retain unverifiable legacy decisions in a lossless quarantine collection that never participates in inheritance, Apply or RMK. | ID-only persistence would either fail on next load or reintroduce ordinal inheritance. New keyless v6 and legacy quarantine round-trip/direct-skip fixtures now pass in the settled 54/54 suite. | Dropping legacy data, accepting ordinal IDs and making unreadable reviews fatal were rejected. | Preserves user-authored legacy content without making it executable and makes new keyless edits round-trip safely. | Yes |
| DEC-028 | 2026-07-14 | Count an RMK entry as eligible only after exact target resolution, safety/staleness checks and overwrite policy all admit a concrete write; preview and completion share that post-exclusion count. | `EligibleEntries` was incremented before ambiguity and `Overwrite=false` exclusions. Ambiguous, non-overwrite and zero-write preview/completion fixtures now pass in the settled 54/54 suite. | Counting candidates before validation or reporting separate undocumented semantics was rejected. | Makes confirmation/result truthfully reflect writable output and prevents false-success state. | Yes |
| DEC-029 | 2026-07-14 | Preserve every readable unmatched legacy keyed record losslessly in quarantine, but require every active v6 record to carry an exact canonical target; neither class may fall back by bare key. | Follow-up audit found a loss-versus-misbinding boundary not completely covered by ISSUE-049/050/057. Byte-identical legacy round-trip and targetless-v6 rejection now pass in repository and workspace consumers. | Dropping unmatched user content or allowing targetless keyed fallback was rejected. | Preserves user-authored data while making unverifiable content non-executable. | Yes |
| DEC-030 | 2026-07-14 | Reject raw or encoded credentials from every provider endpoint component, including user-info, path, query and fragment, before accepting a settings snapshot; repeat validation when constructing the provider. | Follow-up provider audit found that user-info-only checks missed alternate credential carriers and that unsafe save must be atomic. Raw/encoded/path/query/fragment fixtures and a safe query non-false-positive now pass. | Redacting only at log time or partially saving an invalid endpoint was rejected. | Prevents credential persistence/exfiltration without rejecting ordinary safe query parameters. | Yes |
| DEC-031 | 2026-07-14 | Resolve a blank explicit Google endpoint from the provider catalog rather than treating the empty string as a custom or invalid URI. | The provider selector can explicitly choose Google while leaving the endpoint field blank. A fake handler now asserts the canonical catalog URI with no real or paid call. | Requiring users to copy the built-in URL or silently switching providers was rejected. | Restores predictable built-in-provider behavior without changing request contents. | Yes |
| DEC-032 | 2026-07-14 | Make package children depend only on trusted OS roots plus run-owned profiles/temp/artifacts, validate the exact three 8.0.28 `downloadDependencies`, and keep every restore/build/test/run/publish artifact under the run root. | Full E2E exposed missing sanitized `ProgramFiles`, an assets validator aimed at `libraries`, and a `dotnet run` path that ignored run-owned artifacts. Tooling 13/13 and the complete offline package E2E passed after repair. | Reintroducing the ambient environment, accepting library approximations or using repository `bin/obj` was rejected. | Keeps offline runtime provenance and tested/published bytes tied to one isolated run. | Yes |
| DEC-033 | 2026-07-14 | Mark Phase 04 technical `PASS` for the active working snapshot and exact `1.0.1-rc.1` RC while explicitly carrying the unstaged tracked-deletion distinction, untouched historical ZIPs and accepted package P2 hardening into final governance. | Active and exact-RC script scans are zero and package E2E passed, but the Git index still lists two tracked-deleted scripts because no staging/commit is authorized; three of four ignored historical ZIPs contain legacy scripts. | Falsely claiming tracked-tree zero, deleting user-owned artifacts or blocking the active exact-RC result on an unauthorized staging action was rejected. | Honest scope-specific PASS without changing public GitHub state or user-owned history. | Yes |
| DEC-034 | 2026-07-14 | Treat opening a legacy project/review as read-only: apply safe defaults and migration state only in memory, and persist only after an explicit save or before an operation that must consume the migrated state. | Phase 05 byte-hash tests showed that opening/closing must not rewrite a user's working file merely because the C# schema is newer. | Silent autosave on load and using stale disk state for Apply/RMK were both rejected. | Preserves original bytes on inspection while ensuring a user-triggered source-consuming action has durable, bound state. | Yes |
| DEC-035 | 2026-07-14 | Compare Golden and C# provider requests as decoded JSON plus exact system/user UTF-8 hashes; document standard serializer escape spelling as non-functional. Keep generated glossary maximum 40 in the Windows UI because the Golden public UI explicitly passes 40 even though the direct script default is 140. | Isolated exact-Golden AST capture produced identical prompt hashes and a zero-diff common canonical request; raw serializers differ only in equivalent escapes. | Treating byte-level JSON escape case as a functional failure or changing the UI to the unused CLI default was rejected. | Preserves actual public UI/request behavior and leaves an auditable distinction between semantic parity and wire representation. | Yes |
| DEC-036 | 2026-07-14 | Reopen every flushed translation audit JSON/CSV, preserved-translation JSON and ownership marker and validate its structure/content before replacing an existing user-visible artifact. | Phase 05 audit found several outputs used atomic replacement without the required post-flush semantic validation. | Relying only on successful serialization or validating after replacement was rejected. | A malformed/truncated temp artifact cannot displace the last valid file; CSV multiline/quotes and JSON item counts are proven. | Yes |
| DEC-037 | 2026-07-14 | Mark Phase 05 technical `PASS` on synthetic isolated evidence, while carrying actual Excel interactive open, clean-PC compatibility and unsafe host-level disk-exhaustion/process-kill exercises to later manual/adversarial gates. | Strict build 0/0, console 65/65, fixture manifest, XML/XLSX package preservation and exact Golden request parity pass without touching real data. Exhausting the user's disk or killing the actual app would violate host safety. | Converting unrun manual checks to PASS or destructively forcing them on the user's system was rejected. | Phase 06 can proceed; the final verdict still cannot be PASS until later gates state the manual scope honestly. | Yes |
| DEC-038 | 2026-07-14 | Use exact text after CRLF/CR-to-LF normalization for RMK translation-time source history too; do not trim edge or line-end spaces. | A final static sweep found the translation engine's RMK history comparator still used the old whitespace-cleaning helper even though Load/Apply/RMK final actions were already exact. | Treating UI history as cosmetic or relying only on the later apply refusal was rejected. | The review row is demoted and visibly marked changed at the earliest comparison stage, preserving the exact historical/current sources. | Yes |
| DEC-039 | 2026-07-14 | Sanitize XML by Unicode scalar validity: retain valid surrogate pairs/supplementary characters and remove only XML 1.0-forbidden controls, noncharacters and isolated surrogates. | Final Phase 05 source audit showed the previous UTF-16 code-unit regex deleted valid emoji and CJK Extension characters. | Dropping every surrogate or replacing malformed UTF-16 with U+FFFD was rejected. | Prevents silent translation loss while still producing valid XML. | Yes |
| DEC-040 | 2026-07-14 | Accept provider direct-map fallback values only when each value is a JSON string; reject objects, arrays, numbers, booleans and null before validation/apply. | A non-string object containing Korean could stringify and pass the downstream language/safety checks. | Coercing arbitrary JSON with `ToString()` was rejected. | Fails closed before malformed provider data can become an applicable translation. | Yes |
| DEC-041 | 2026-07-14 | Emit preserved-translation schema v2 with exact source hash/text, unresolved-change state and past-source history; validate it strictly, treat provenance-free v1 as review-required, and carry history through approval and repeated refreshes without inventing unavailable history. | Adversarial MissingOnly audits reproduced stale reuse, past-source loss after temp cleanup, re-demotion after approval, history loss on a second refresh and A→B→C history sticking at A. | Depending solely on the previous decision store, silently reusing v1, or labeling the current source as unknown history were rejected. | Preserves translations and source history, retranslates stale data, keeps explicit approval durable, and fails closed when provenance is unavailable. | Yes |
| DEC-042 | 2026-07-14 | Charge reported/estimated usage for every successful HTTP response before validating its translation body, guard non-object root/usage metadata, and require the response ID set to equal the requested ID set exactly. | A malformed 2xx could consume tokens without decrementing the daily budget, non-object JSON could bypass the retry catch through `InvalidOperationException`, and unexpected IDs were previously ignored. | Charging only accepted results, assuming every JSON value was an object and accepting a superset were rejected. | Keeps retry cost limits conservative and malformed responses retryable while preventing over-complete output from entering the pipeline. | Yes |
| DEC-043 | 2026-07-14 | Upgrade any workbook shape the RMK reader accepts by appending only missing identifier/class/node/Required Mods header columns after the last used worksheet column. | The reader accepted identifier-only or class+node legacy headers while the writer required all metadata columns and therefore could not update the same file. | Narrowing the reader or overwriting existing/extra columns was rejected. | Restores read/write symmetry while preserving extra columns and package parts. | Yes |
| DEC-044 | 2026-07-14 | Preserve the Golden information hierarchy and every required action at narrow/high-DPI sizes by stacking or scrolling only the affected regions; do not remove controls or redesign the workflow. | Matched minimum and actual-125 captures exposed fixed-height header/editor pressure and narrow dashboard action pressure. Final 15+20 captures report zero missing/clipped controls or text. | Hiding lower-priority actions, shrinking text or redesigning the workspace was rejected. | Maintains behavior/UI compatibility while making layout DPI-aware and reversible. | Yes |
| DEC-045 | 2026-07-14 | Treat pixel metrics as descriptive and require a human structural/flow disposition for every matched Golden/C# state, backed by side-by-side, overlay, diff, mask, accessibility and interaction evidence. | Native C# controls/font rasterization make whole-screen ratios 0.497327-0.999999 even when the required structure and state agree. | An arbitrary pixel threshold or visual sign-off without reproducible artifacts was rejected. | Prevents false failure from rendering differences and false PASS from screenshots that omit behavior. | Yes |
| DEC-046 | 2026-07-14 | Render progress/errors through a bounded privacy-safe presentation object and make capture failures omit exception messages, stacks and paths. | Final UI evidence must not retain real paths, credentials or raw provider/source detail; the safe-failure probe and Phase 06 text scan are clean. | Regex-only post-capture cleanup or retaining raw failure logs was rejected. | Reduces disclosure at the source while keeping actionable error category/HRESULT/time evidence. | Yes |
| DEC-047 | 2026-07-14 | Accept provider endpoints only as HTTPS URLs with no fragment, a strict `api-version`/`format=json`/`version` query allowlist and no raw or recursively encoded credential material; keep settings in correction state until both primary and backup stores are clean. | Phase 07 provider/settings fixtures and the `--provider-url-security` UI harness verify blocked unsafe drafts/stored values, unchanged storage on rejection, safe unknown-field round-trip and correction cleanup without a real provider call. | Host-only checks, unrestricted query strings, redaction after persistence and silently accepting a safe primary beside an unsafe backup were rejected. | Prevents credential-bearing endpoint persistence/exfiltration and makes the recovery state visible. | Yes |
| DEC-048 | 2026-07-14 | Bind security-sensitive output, backup and recovery mutations to a trusted write boundary and identity-plus-SHA-256 compare-and-swap, preserving concurrent winners and explicit recovery evidence instead of overwriting them. | Phase 07 directory/leaf substitution, prepared/target/backup/post-commit race, sync/async/LanguageData rollback and same-content/different-identity fixtures pass. | Lexical path checks, path-only snapshots and unconditional rollback replacement were rejected. | Prevents tested alias/race classes from redirecting writes or destroying newer user data. | Yes |
| DEC-049 | 2026-07-14 | Treat the selected RMK work clone and its Builder as trusted executable input, not as sandboxed content: pin the confirmed EXE identity/length/hash and contain lifetime/environment/output, while explicitly disclosing that adjacent DLL/config/clone content and current-user filesystem/network authority remain trusted. | Hash-swap, timeout, output-cap and transactional process fixtures pass, but containment cannot authenticate the whole clone or remove the Builder's user authority. | Claiming a sandbox, silently executing an unconfirmed binary or attempting an incompatible application-container redesign for this RC were rejected. | Narrows and documents the execution boundary without overstating protection. | Yes |
| DEC-050 | 2026-07-14 | Reject active or executable XLSX/ZIP content, external/escaping relationships and formula-bearing translation surfaces before read or update, while preserving allowed passive package metadata. | Both Phase 07 native groups cover macro/OLE/VML/active content types, formulas, hostile relationships, archive bounds and rejected-update byte preservation. | Sanitizing unknown active content in place or allowing formulas because Excel may display them as text were rejected. | Fails closed against active-content execution and spreadsheet-formula injection without discarding compatible passive metadata. | Yes |
| DEC-051 | 2026-07-14 | Apply explicit per-item and aggregate byte/file/row/character/candidate limits plus cancellation to every challenged untrusted discovery/read surface, and neutralize formula-leading comparison CSV cells. | Phase 07 resource groups cover JSON, glossary, XML, preserved/comparison data, Steam/RMK discovery and language traversal limits; the settled 71/71 suite includes CSV neutralization. | Unbounded convenience reads, post-allocation limits and relying on spreadsheet UI warnings were rejected. | Bounds memory/CPU/disk amplification and prevents exported CSV from becoming executable spreadsheet input. | Yes |
| DEC-052 | 2026-07-14 | Treat review-decision reads as potential writes because backup recovery can mutate storage; reject network, lexical/canonical Workshop and application-external review roots before probing, and permit recovery only under the configured application reviews root. | Current/previous workspace, Activity, repository, Apply and RMK backup-only denied-root fixtures produce no primary/corrupt leaf; trusted in-root recovery preserves the valid backup and emits notice. | Calling recovery a read-only operation, probing first and validating later, or trusting any stored review path were rejected. | Preserves Workshop immutability and prevents recovery writes outside application-owned review storage. | Yes |
| DEC-053 | 2026-07-14 | Separate Phase 07's current-source technical PASS from the overall RC/public-distribution verdict: SEC-009 glossary redistribution rights and SEC-010 authoritative runtime notices must be resolved and verified before any overall PASS or public-distributable claim. | The glossary self-test proves structure only; no redistribution grant is recorded for 3,833 official-derived observations, and the self-contained package allowlist has no reviewed runtime notice file. The Phase 04 ZIP is stale. | Treating MIT code licensing as permission for derived text, inferring notices from package verification, or reusing the stale ZIP were rejected. | Prevents an unsupported legal/supply-chain release claim while allowing Phase 08 technical work to proceed. | Yes |
| DEC-054 | 2026-07-14 | Authorize crash recovery only from an injected app-owned authority store and durably predeclare every forward/rollback endpoint before target-tree mutation. | Root-local manifest forgery and repeated child-kill audits exposed ISSUE-091..093. Focused recovery and three 80/80 suites now converge while preserving unknown concurrent leaves. | Trusting a manifest beside user content, reconstructing ownership heuristically after a crash, or silently deleting unknown intermediates were rejected. | Removes forged-delete and non-convergent-recovery P0/P1 paths while preserving last-good data. | Yes |
| DEC-055 | 2026-07-14 | Bind state ownership across processes with a canonical-root file lease, project revision CAS, and one pinned backup byte sequence for both deserialize and restore. | Cross-process lease schema v5, project repository CAS, and AtomicJson A-to-B substitution fixtures all pass. | `Local\\` semaphore-only ownership, last-writer-wins saves and path rereads after validation were rejected. | Prevents cross-session double-open, stale-save loss and split-brain backup recovery. | Yes |
| DEC-056 | 2026-07-14 | Run RMK Builder only from an isolated app-owned staging tree, validate a sealed exact input/output manifest, recover only uniquely owned run residues, and publish through the held FileTransaction boundary. | Builder transaction focused tests, strict ModList validation, stage mutation/injection/depth/live-race cases and 80/80 x3 pass; independent audit found P0/P1=0. | Executing in the live clone, wildcard cleanup, trusting child-declared output, or treating the builder as sandboxed were rejected. | Protects the live RMK clone and preserves unknown residue; ISSUE-086 remains a disclosed selected-clone trust boundary. | Yes |
| DEC-057 | 2026-07-14 | Pin exact-directory cleanup against parent rename with a validated delete-on-close child lease, and disclose the remaining path-relative creation/power-loss residue as P2. | The parent-rename fixture passes repeatedly; focused cleanup and 80/80 x3 pass; independent read-only review found no P0/P1 and identified ISSUE-097. | Relying only on a parent handle on this Windows host, broad path deletion, or claiming perfect crash cleanup were rejected. | Closes the demonstrated namespace-swap deletion race without hiding a non-destructive residual. | Yes |
| DEC-058 | 2026-07-14 | Treat Golden UI/RMK numbers as reference-only when process and operation boundaries differ; apply absolute candidate thresholds and carry uninstrumented package/manual boundaries as BLOCKED evidence, not fabricated ratios. | Five actual WinForms reports and candidate benchmark pass all absolute gates; the report correctly remains evidence-completeness BLOCKED pending package/startup/idle/manual proof. | Comparing Core queries to WinForms, in-process RMK to a new PowerShell process, or inferring package behavior from source harnesses were rejected. | Prevents false regression claims while retaining measurable 200/250 ms and 15% gates. | Yes |
| DEC-059 | 2026-07-14 | Close Phase 08 as a current-source technical PASS only, while leaving the overall RC verdict unavailable until Phase 09 package evidence, Phase 10 audit, SEC-009/010 and manual checks are resolved or explicitly blocked. | Strict build 0/0, focused gates, 80/80 x3, UI/lifecycle/CAS/MainForm evidence and automated performance gates pass; package and legal/manual evidence remain open. | Declaring overall PASS now or stopping before producing a local RC were rejected. | Allows ordered progress without weakening the final governance contract. | Yes |
| DEC-060 | 2026-07-14 | Exclude `glossary.generated.ko.json` from the local RC while leaving SEC-009 and the affected Golden parity explicitly BLOCKED. | No redistribution grant is recorded for 3,833 official-derived observations; the exact 14-file ZIP contains no generated glossary. | Shipping the disputed bytes, silently treating omission as parity, or inventing a license were rejected. | Avoids direct redistribution in this RC without hiding the loss of bundled suggestions and glossary-bearing request parity. | Yes |
| DEC-061 | 2026-07-14 | Resolve SEC-010 only for the exact package by including and manifest-binding four authoritative .NET 8.0.28 notice/license files. | Both Phase 09 reproducible ZIPs contained the same four files; all three final v5 ZIPs and `THIRD_PARTY_NOTICES.md` inventory revalidate every hash against the exact manifest. | Generic attribution text or a notice copied without exact package verification was rejected. | Makes the self-contained runtime obligations auditable and forces revalidation after any runtime/package change. | Yes |
| DEC-062 | 2026-07-14 | Place package workspaces directly under a short run-owned `%TEMP%\\RwatPkg-<GUID>` root and isolate child TEMP/profile/AppData beneath it. | Long inherited TEMP/profile paths caused a real package failure; tooling self-test 17/17 covers ownership, length and cleanup. | Relaxing path checks, using user profile roots, or broad cleanup was rejected. | Preserves Windows compatibility and user-data isolation while keeping cleanup exact and recoverable. | Yes |
| DEC-063 | 2026-07-14 | Validate duplicate-instance dialogs semantically and PID-locally, permitting the observed standard-button ID alias only when title, body, button count, enabled state and process ownership are exact. | Two Phase 09 package smokes and all three final v5 package smokes pass with exit 2, responsive holder, zero descendants and subsequent data-root reacquisition. | Clicking by screen position, accepting arbitrary dialogs, or depending on one native ID was rejected. | Keeps the smoke portable without weakening its security or behavior assertion. | Yes |
| DEC-064 | 2026-07-14 | Make local package identity independent of the run root by applying one complete-root `PathMap` to build and publish; retain uncompressed single-file bundling as a separate transparent package policy. | Equal-length pre-fix EXEs had 2,655 differing byte positions, fully explained by 42 occurrences of the absolute-path-derived file-local prefix plus deterministic PE/MVID/bundle cascades. An uncompressed but unmapped pair still differed; after PathMap, two distinct clean roots produce byte-identical ZIP, EXE and 14 extracted files. | Treating timestamps or compression alone as the cause, post-build binary rewriting, or ignoring EXE differences was rejected. | Produces a reproducible artifact without changing product runtime behavior or falsifying manifest creation time. | Yes |
| DEC-065 | 2026-07-14 | Close Phase 09 as automated PASS but overall BLOCKED, and freeze the package-input snapshot before later governance documentation changes. | Exact RC allowlist/hash/reproducibility/smoke/performance/Defender/runtime-notice gates pass; SEC-009 and clean-PC/Excel/high-DPI/assistive-technology/user-visual checks remain unresolved. | Declaring overall PASS, rebuilding solely for governance-only docs, or treating unrun manual checks as PASS was rejected. | Preserves an exact technical RC and an honest final-gate boundary. | Yes |
| DEC-066 | 2026-07-14 | Bind every Apply and RMK preview to execution with a deterministic SHA-256 plan fingerprint over normalized roots, options, counts, exclusions and exact decision/source/output/workbook baselines; fail closed on any drift and reverify output baselines inside the write boundary. | Independent Phase 10 review showed a fresh execution plan could differ from the preview the user confirmed. Eight focused drift/unchanged/fail-before-write cases plus the final 82-test suite pass. | Freezing only row counts, trusting a second equivalent-looking preview, or allowing an automatic replan was rejected. | The mutation is exactly the plan confirmed default-No; concurrent/user changes are preserved and require a new preview. | Yes |
| DEC-067 | 2026-07-14 | Present recovery using exact bounded target/corrupt leaf names while omitting parent paths, and centrally drain queued notices after every UI action. | Independent review found filename loss and paths that could leave a notice queued. The final presentation probe covers single/multiple/no-copy/control-character cases. | Showing a full path, a generic “corrupt copy” label, or relying on each caller to remember draining was rejected. | Gives the user actionable identity without exposing profile paths or silently hiding recovery. | Yes |
| DEC-068 | 2026-07-14 | Treat per-entry Zip64 sentinels/extra field, split-disk metadata, malformed extras and central-directory count/trailing inconsistency as fail-closed archive errors before read or update. | The prior aggregate EOCD checks did not cover every entry-level encoding. Eight hostile reader/writer fixtures preserve original bytes and leave no backup/temp. | Partially interpreting Zip64, ignoring unknown malformed metadata or trusting `ZipArchive` alone was rejected. | Narrows the accepted XLSX/ZIP grammar and closes a High archive-boundary gap without altering valid packages. | Yes |
| DEC-069 | 2026-07-14 | Compare package reproducibility on the archive, EXE and all 14 payload files byte-for-byte; compare the out-of-band manifest after omitting only truthful `createdUtc`. | From the frozen 250-file input `C279F6F6...3A006`, three final v5 runs have identical ZIP `2302E877...B7C1`, EXE `9EEB6A80...19C8` and all payloads. Raw manifests differ only in truthful `createdUtc`; the normalized hash is identical. | Falsifying the timestamp, ignoring all manifest fields or calling raw timestamp variance a product failure was rejected. | Keeps artifact determinism strict while retaining honest creation metadata. | Yes |
| DEC-070 | 2026-07-14 | Record the empty application `PackageReference` graph as N/A for NuGet advisories when the cleared-source CLI fails, and separately use official Microsoft servicing/support evidence for the embedded .NET 8.0.28 runtime. | `dotnet list --vulnerable` exits with `Attempted to divide by zero` on the empty graph; Microsoft identifies 8.0.28 as the current .NET 8 servicing release with support through 2026-11-10. | Calling the CLI failure PASS or treating application packages and embedded runtime as the same inventory was rejected. | Avoids a fabricated dependency PASS and establishes the exact runtime support disposition from the authoritative source. | Yes |
| DEC-071 | 2026-07-14 | Preserve pre-session temporary directories and treat the persistent package coordination leaf as idle when it can be exclusively opened; residue PASS is scoped to current-run exact roots and process/journal state. | The first broad query falsely counted six 2026-07-12 roots and the existence of a zero-byte `OpenOrCreate` lock leaf. Final v5 smoke cleanup is 3/3, three failed-run roots were ownership-validated and cleaned, current product/build/work/journal counts are 0 and exclusive open succeeds. | Deleting user-owned directories or requiring the persistent coordination leaf to be absent was rejected. | Protects user data while making the cleanup assertion accurate and reproducible. | Yes |
| DEC-072 | 2026-07-14 | Issue final `BLOCKED`, not PASS or FAIL. | The four original local evidence gaps are closed, completed final-v5 gates pass at 82/82 x3 plus one separate tooling 18/18, and exact-v5 idle memory is measured. SEC-009, equivalent Golden process boundaries, project/Apply/RMK cancellation and packaged forced-exit breadth, Phase 10 manual end-to-end flows and the named clean-PC/Excel/DPI/assistive/native-dialog/privacy/visual/SmartScreen checks remain unverified or require external conditions. | PASS would invent evidence; FAIL would misstate the successful completed local gates and usable exact RC. | Produces an honest local RC handoff without authorizing public distribution. | Yes |
| DEC-073 | 2026-07-14 | Require both the source translation and every candidate target to pass the shared safety predicate before an identical-source bulk update. | The Phase 10 local-gap fixture showed that user confirmation alone was not a sufficient safety boundary. The focused unsafe-source/unsafe-target regression, final 82/82 x3 and three exact-v5 package runs pass. | Trusting the selected source, validating only targets, or changing unsafe/unrelated rows and relying on final Apply rejection were rejected. | Prevents bulk editing from bypassing structural/token/source safety while preserving status and source history for excluded rows. | Yes |
| DEC-074 | 2026-07-14 | Validate package-smoke process ancestry with creation-time identity on every parent-child edge and fail closed when an identity cannot be read. | Toolhelp can retain a stale numeric parent PID after exit; a reused PID could otherwise attach unrelated older system-process branches. `ProcessTree.PidReuseIdentity`, tooling 18/18 and three final v5 smokes pass. | Trusting numeric PID/PPID alone, ignoring unreadable candidate identity, or allowlisting process names were rejected. | Prevents false descendant evidence without killing or otherwise mutating unrelated processes. | Yes |
| DEC-075 | 2026-07-14 | Retry exact-directory quarantine rename only for transient Win32 access/sharing/lock errors 5/32/33, at most eight times, while retaining the same pinned parent/root handles and revalidating identity on every attempt. | Final-v4 full/package runs exposed transient error 5 and failed 81/82 while preserving the owned stage. A focused two-attempt injection, final 82/82 x3 and three final-v5 packages pass. | One-shot failure, broad retry of arbitrary errors, reopening by path, or deleting a preserved quarantine were rejected. | Removes a demonstrated cleanup flake without weakening fail-closed ownership, namespace or content checks. | Yes |
| DEC-076 | 2026-07-14 | Freeze the post-remediation v5 package input as the final local-RC boundary and treat every earlier Phase 10 snapshot, ZIP and EXE as superseded. | The final inventory contains 250 files with combined SHA-256 `C279F6F6...3A006`; three clean runs reproduce ZIP `2302E877...B7C1`, EXE `9EEB6A80...19C8` and all 14 payloads after duplicate-safety, PID-identity and cleanup-retry remediation. | Reusing v1-v4 Phase 10 artifacts, mixing evidence across package identities, or rebuilding for later governance-only text changes were rejected. | Gives every final technical claim one exact source and binary identity while preserving truthful later documentation drift. | Yes |
